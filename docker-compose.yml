## version: "3.9"
## Tener en cuenta que cada microservicio tiene su propio Dockerfile en su respectiva carpeta.
## Se podria agregar un docker file por cada microservicio, inclusive (ejemplo creacion de trenes con docker file y asi)
services:
  python-api:
    build:
      context: ./Backend/python
      dockerfile: Dockerfile
    container_name: railvision-python
    ports:
      - "8000:8000"
    env_file:
      - ./Backend/python/RailVision/.env
    volumes:
      - ./Backend/python/RailVision:/app
    # depends_on:
    #   - db
    networks:
      - railvision-network

  # ---------------------------
  # BACKEND JAVA
  # ---------------------------
  java-api:
    build:
      context: ./Backend/java
      dockerfile: Dockerfile
    container_name: railvision-java
    ports:
      - "8080:8080"
    # depends_on:
    #   - db
    networks:
      - railvision-network

  # ---------------------------
  # FRONTEND ANGULAR
  # ---------------------------
  frontend:
    build:
      context: ./Frontend/angular
      dockerfile: Dockerfile
    container_name: railvision-frontend
    ports:
      - "4200:80"
    depends_on:
      - python-api
      - java-api
    networks:
      - railvision-network

#   # -------------------------
#   # BASE DE DATOS LOCAL (solo para pruebas)
#   # -------------------------
#   db:
#     image: postgres:15
#     container_name: railvision-db
#     restart: always
#     environment:
#       POSTGRES_DB: postgres
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#     ports:
#       - "5432:5432"
#     volumes:
#       - db_data:/var/lib/postgresql/data
#     networks:
#       - railvision-network
#     # 🚫 desactívalo temporalmente si usas Supabase
#     # para evitar conflicto con el puerto 5432

networks:
  railvision-network:
    driver: bridge

# volumes:
#   db_data: